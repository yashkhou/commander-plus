# Security policy

Do not report real credentials in an issue. Reproduce security problems with synthetic values only.

The public core intentionally omits privileged production adapters. If you extend it with shell, browser or desktop execution, use explicit allowlists, narrow filesystem scopes and human-visible audit logs.
